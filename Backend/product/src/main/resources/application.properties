spring.application.name=product-service
server.port = 9090
# JDBC URL for AWS RDS MySQL
spring.datasource.url=jdbc:mysql://product-db.cqh86uu4i9cy.us-east-1.rds.amazonaws.com:3306/products?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
#product-db.cqh86uu4i9cy.us-east-1.rds.amazonaws.com
# MySQL credentials
spring.datasource.username=admin
spring.datasource.password=12345678

eureka.client.service-url.defaultZone=http://65.2.99.60:8761/eureka/
# Tells the service where to find the registry
# Recommended for local development to use IP instead of hostname
eureka.instance.prefer-ip-address=true

# Connection pool settings (recommended for production)
#spring.datasource.hikari.maximum-pool-size=10
#spring.datasource.hikari.minimum-idle=5
#spring.datasource.hikari.connection-timeout=30000
#spring.datasource.hikari.idle-timeout=600000
#spring.datasource.hikari.max-lifetime=1800000

# JPA & Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
